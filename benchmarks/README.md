# Benchmarks Suite for SIH26158

This directory contains standardized benchmark harnesses and comparative evaluation scripts.

## Standards
- All benchmark executions are governed by the rules in `docs/experiments/EXPERIMENT_PROTOCOL.md`.
- No benchmark result may be logged without explicit dataset name, ground-truth reference, metric name, and reproducible calculation script.
- Benchmark outputs are saved with complete hardware and configuration manifests to ensure 100% reproducibility.
